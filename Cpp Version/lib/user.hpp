#include <string>
#define string std::string

class User {
    private: 
        string id; 
        string username; 
        string password;
        string name; 
        string conttact; 
        class Address {
            private: 
                string city, district, ward, street, number; 
        } address;

    public:  
        
};